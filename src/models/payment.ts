export enum TransactionStatus {
  PENDING = 'PENDING',
  APPROVED = 'APPROVED',
  DECLINED = 'DECLINED',
  ERROR = 'ERROR',
}

export enum CurrencyCode {
  COP = 'COP',
  USD = 'USD',
  EUR = 'EUR',
}